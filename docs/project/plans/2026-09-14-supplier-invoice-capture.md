# Satınalma faturası kalıcı taslak yakalama

- MP-04 maliyet kaynağı bağımlılığı / PUR-DRAFT-001; R3; validating; 14 Eylül 2026.
- Amaç: Kaynak fatura başlığı ve satırlarını kalıcı, company-scoped immutable ilk taslak olarak saklamak.
- Ready: Purchasing kaynak sözleşmeleri var; gerçek fatura tablosu yok. Bu dar bağımlılık MP-05 workflow kabulü değildir.
- Okunanlar: AGENTS, MASTER_PLAN MP-04, Purchasing, veri/repository mimarisi, migration kalite ilkeleri, 0018 party account ve 0051 draft örneği.
- Sahip: Teknik Codex; ürün/muhasebe atanmadı.

## Kapsam/done when

PUR-DRAFT-003 / MP-04 / R3 (devam dilimi): kayıtlı capture için cari payable/currency ve mevcut item/company active/UOM/quantity-scale kontrollerini bağla. Ready: DEC-MP01-003/008 tek dövizli ayrı cari ve inventory master kuralları mevcut; cari aktiflik/PartyRole lifecycle tablosu yok, bunlar doğrulanmış sayılmayacak. Parties ve Inventory kendi tablolarını published transaction participant üzerinden okur; Bootstrap birleştirir. `purchasing.invoice.view` yetkili iç okuma, caller RC, canonical item locks, audit/savepoint; finansal hareket/state transition yok. Başarı yalnız bu sınırlı kontrollerin geçtiğidir; finalize izni değildir. Yeni migration/dependency/endpoint/istemci yok, rollback çağrıyı kapatır. Done when: kontroller, negatif DB senaryoları, dar compile ve doküman; runtime kanıt gelene kadar validating. Okunan ek sözleşmeler: Parties AGENTS, party modülü, veri mimarisi, ortak iş akışları, repository sınırları, test kadansı ve mevcut master loader. Sorumlular önceki gibi; cari aktiflik gelecekteki teknik lifecycle işi, kullanıcıdan yeni karar gerektirmiyor.

Taslak kimliği, cari hesap referansı, dış belge numarası/tarihi/dövizi ve 1–500 item/UOM/base quantity/net amount satırı tutulur. Net tutar işlem dövizindedir; KDV, kur, eligible functional cost veya GL sonucu hesaplanmaz. İlk yakalama immutable'dır; revision/cancel/finalize sonraki lifecycle işidir. Taslak bilinçli olarak supplier role/currency/master-active/matching doğrulanmış sayılmaz. FK tenant/company/item/UOM varlığını korur; resmi belge tekilliği finalization'da uygulanır, ilk capture key tekrarı exact fingerprint ile idempotenttir.

0053 expand-only migration; normalized header/line, deferred line-count/total check, immutable triggers, forced RLS, app SELECT-only. İç writer permission `purchasing.invoice.create` ile caller RC transaction + advisory lock + savepoint kullanır; replay exact içerikse orijinal recorded time döner, farklıysa conflict. Bootstrap audit aynı transaction'da. Runtime INSERT grant/HTTP/DI yok.

Test: create/replay/conflict, diğer şirket not-found/permission, satır bütünlüğü ve rollback. Yeni paket yok; Npgsql mevcut sabit sürüm. Migration boş/verili DB ve runtime testleri MP-04 kapısında; compile başarı kabul değildir. Rollback yeni feature çağrısını kapatır; mevcut tabloları silmez. Taslak fatura maliyet source interface'ine bağlanmaz, kesinleşmiş source gibi yayımlanmaz. Commit/push yok.

## 14 Eylül ilerleme

Test/teslim oturumu: kullanıcı tam test ve commit/push istedi. Capture master-check başarısızlığının başarılı read audit bırakmadığı DB assertion eklendi. Birikmiş C# format/import bulguları formatter ile düzeltildi; iki eski integration fixture'ın whitespace bulguları da giderildi. Son Integration Release compile 24,08 sn, 0 uyarı/hata. Web kurulu araçlarla Vitest: 2 dosya/6 test geçti; API client TypeScript derlemesi sonrası ESLint ve zorlanmış TypeScript build geçti; Vite production build geçti. `pnpm verify` otomatik bağımlılık yeniden kurulumunda NO_TTY ile durdu; modules dizini silinmedi. Tam solution build API/OpenAPI assembly yüklemesinde, unit runtime ise test assembly yüklemesinde CodeIntegrity 0x800711C7 ile engellendi. Docker elevated read check: Linux engine pipe yok, DB/migration/restore koşulmadı. Android ve tam E2E koşulmadı. Bu sonuçlar tam MP kapısı başarısı değildir.

Windows event 3077 ve SAC registry state=1 doğrulandı. Kullanıcı sistem geneli Smart App Control kapatmayı ayrıca açıkça onayladı; yalnız ilgili ayarı 0 yapma elevated çağrısı registry access denied verdi. Koruma kapatılmadı, Defender/firewall değiştirilmedi, yeniden başlatılmadı. Kullanıcının Windows Güvenliği arayüzünde ayarı değiştirmesi ve Docker Desktop'ı başlatması bekleniyor. Sonrasında tüm backend/DB/migration/golden kapıları tekrar koşulacak. Commit/push, bu açık test borçlarıyla geliştirme checkpoint'idir; production veya MP-04 tamamlandı kabulü değildir.

PUR-DRAFT-003 uygulandı: Parties-owned payable/currency participant, Inventory-owned bounded current item participant ve Bootstrap capture master-check orkestrasyonu eklendi. Modüller arası tablo erişimi eklenmedi; yalnız Bootstrap published participant çağırır. Master doğrulama/audit aynı RC transaction/savepoint'te; değişebilir masterlar FOR SHARE kilitli kalır, capture geçmiş okuması aktiflikten bağımsızdır. Cari aktiflik modeli yokluğu bilinçli açık kalır. Yeni migration/grant/bağımlılık/HTTP yok.

Ek senaryolar: mevcut capture başarı, yanlış döviz/payable olmayan hesap, başka şirket hesabı, eksik permission, yanlış item/UOM, 501 satır sınırı, ürün/şirket pasifliği, kesir kabul etmeyen ürün ve pasiflik sonrası tarihsel okuma. Senaryolar derlendi, çalıştırılmadı. Integration Release `--no-restore -v:q` 10,28 sn, 0 uyarı/hata; diff check içerik hatası yok (önceki lock CRLF uyarıları). Runtime finansal/güvenlik ve app-role RLS/concurrency kanıtı mevcut CodeIntegrity engeli ve MP kapanış kadansı nedeniyle açık; yeniden koşu tetikleyicisi ortam engelinin kalkması/MP-04 birleşik kapısıdır. Production kabulü yok, master kapısı değişmedi. Sonraki somut eksik: cari aktiflik/lifecycle sözleşmesi; sonra fatura doğrulama/state ve gerçek finalized source. Commit/push yok.

İkinci dilim / PUR-DRAFT-002 (R3): persisted capture okuma. Ready: 0053 header/line sözleşmesi ve create/audit katmanı mevcut; migration henüz uygulanmadı. İç reader `purchasing.invoice.view`, exact tenant/company filtreleri ve caller RC transaction ile tek LEFT JOIN statement snapshot'ından en fazla 501 satır okur. Orijinal yazarla capture yeniden kurulur; sürüm, satır sayısı, toplam ve canonical fingerprint doğrulanmadan sonuç dönmez. Eksik/başka kapsamdaki kimlik null; bozuk içerik genel integrity hatasıdır. Bootstrap okuma/not-found audit'i aynı transaction ve savepoint içinde; belge numarası/tutar audit'e taşınmaz. Yeni migration, HTTP/OpenAPI, istemci, bağımlılık veya runtime grant yok. Bu okuma kaydı finalized source hâline getirmez. Rollback çağrı yolunu kapatmaktır; veri değişmez.

Ek DB senaryoları: round-trip, yazarın okuyucudan bağımsız korunması, create yetkisinin view yerine geçmemesi, başka tenant/company not-found, audit actor uyuşmazlığı, audit SQL hatası sonrası transaction devamı, eksik satır ve doğru toplamlı yanlış fingerprint reddi. Owner fixture içindeki uygulama filtreleri sınanır; gerçek app-role RLS çalıştırma kanıtı yerine geçmez. Runtime güvenlik/finansal testler MP-04 kapısına ertelidir; bu nedenle dilim validating kalır.

Okuma dilimi doğrulaması: `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` 10,09 sn, sıfır uyarı/hata. Bu yalnız derlemedir; DB senaryoları çalıştırılmadı, 0053 uygulanmadı. `git diff --check` içerik hatası vermedi; mevcut lock dosyalarının satır sonu uyarıları devam ediyor. Güvenlik/finansal runtime doğruluğu henüz kanıtlanmadığı için production açılışı uygun değildir. Sonraki adım: capture sonrası doğrulama/lifecycle için yayımlanmış cari ve stok master sözleşmelerini kullanarak eksik uygunluk kontrollerini bağlamak; finalized maliyet üreticisine yalnız doğrulanmış kaynak aktarılacak. Commit/push yapılmadı.

Contracts capture modeli (immutable sürüm 1, bounded/exact satırlar, canonical fingerprint), yeni Purchasing.Infrastructure, 0053 migration ve caller-RC writer eklendi. Bootstrap audit wrapper create/replay sonucuyla atomik çalışır. Solution/Bootstrap/Integration/Architecture proje referansları güncellendi. İlk Integration compile eksik doğrudan Purchasing.Infrastructure referansı nedeniyle CS0234 verdi; referanslar eklenip mevcut yerel paket cache'iyle offline restore yapıldı. Paket sürümü/güvenlik kapısı değiştirilmedi; bu restore güncel zafiyet taraması değildir.

Owner DB fixture senaryoları create/replay/timestamp, conflict, permission eksiği, negatif/aşırı hassas net tutar, audit hatasında header rollback, line FK hatasında rollback, başka şirketteki cari FK reddi, eksik satırın deferred commit reddi, immutable UPDATE reddi ve forced RLS/SELECT-only privilege metadata kontrolünü kapsar. Bunlar yazıldı; çalıştırılmadı. Gerçek app-role RLS negatif okuma ve concurrency/boş-verili migration koşusu MP-04 kapanışında hâlâ zorunludur. Capture kayıtları bu oturumda DB'ye uygulanmadı, source/stock/GL kesinleştirme tamamlanmadı.

Komutlar: `dotnet restore KaguERP.slnx --packages C:\Users\ahmet\.nuget\packages --source C:\Users\ahmet\.nuget\packages -v:q` başarılı. Son `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` 9,55 sn; Migrator Release compile 1,33 sn, sıfır uyarı/hata. Sürüm property CA1822 bulgusu auto-property ile düzeltildi, analizör kapatılmadı. Son SQL incelemesinde fixture cari hesabına 0030'un zorunlu `balance_side=2` alanı eklendi; SQL metni henüz runtime doğrulanmadı. Diff check içerik hatası yok; önceki lock dosyalarında CRLF/LF uyarıları var. Commit/push yok.

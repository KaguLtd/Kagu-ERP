# MP-04 Inventory Reservation Lifecycle Foundation

- **Amaç:** Satış talebine ayrılan miktarı stok hareketinden ayrı tutan lifecycle, yetki, kapasite, atomik reservation/result ve aynı-transaction audit zincirini kurmak; runtime açılışını ortak stok koruması ve test kapısına bağlamak.
- **Master fazı ve kapısı:** MP-04 / eşzamanlı reservation ve miktar korunumu.
- **Risk sınıfı:** R4 — stok kullanılabilirliği, depo kapsamı ve satış-sevk zinciri.
- **Durum:** in-progress.
- **Sahip:** Ürün/muhasebe sahipleri `atanmadı`; teknik uygulama Codex.
- **Başlangıç:** 5 Eylül 2026.
- **İlgili requirement ID'leri:** `INV-RES-001`–`INV-RES-014`, `INV-CAP-001`–`INV-CAP-002`, `INV-BLK-001`, `INV-AUTH-001`, `SALES-ORD-002A`, `SALES-RES-001`–`SALES-RES-007`, `SALES-FUL-001`.
- **Okunan belgeler:** `MASTER_PLAN.md`, `docs/modules/04-items-inventory.md`, `docs/modules/05-sales.md`, `docs/00-foundation/07-cross-cutting-workflows.md`.
- **Definition of Ready sonucu:** DEC-MP01-025 kapsamında internal confirm→reservation ve cancel→release composition kodu hazır, runtime kanıtı bekliyor. Public yol için gateway/DTO/OpenAPI, mevcut permission'ların runtime eşlemesi, SQL grant ve toplu test kapısı tamamlanmalıdır. DEC-MP01-011 maliyet/GL ve backdate posting kararları internal miktar composition'ını bloklamaz; onları çözülmüş saymaz.

## Kapsam

### Dahil

- Request/demand/position kilitleri, immutable retry, kısmi kapasite tahsisi ve atomik creation/result persistence.
- Gerçek Sales producer'ının aynı transaction'a bağlanması ve audit hatasında birlikte rollback.
- Ortak reservation/transfer kapasite ve master koruması; confirmed tek sipariş için bütün satırları atomik tutan internal batch.
- Versioned demand source type/id/line/version kimliği.
- Exact tenant/company/item/warehouse/base-UOM ve pozitif `numeric(20,6)` rezerv miktarı.
- `active → partially_consumed → consumed | released | expired` lifecycle.
- Append-only event için previous/new version+status, exact consume miktarı, actor, correlation ve PostgreSQL-safe UTC occurrence.
- Remaining miktarının yalnız state ve exact decimal değerlerden türetilmesi.
- Over-consumption, terminal state mutation, gerekçesiz release ve erken expiry fail-closed negatifleri.
- `inventory.reservation.create`, company scope ve actor-bound authoritative warehouse evidence isteyen
  authorization candidate; farklı actor/company/depo evidence'ı fail-closed kalır.
- Üretici modülün published contract adaptöründen yüklenecek exact source version, item, base-UOM ve
  azami reservable quantity demand evidence'ı; stale version ve demand üstü miktar fail-closed kalır.
- Sales-owned confirmed-order snapshot'ını, dönen scope ve version bağlamını yeniden doğrulayarak
  Inventory-owned reservation demand evidence'a çeviren modüller arası adaptör.

### Dahil değil

- Dışarıya açık available sorgusu ve runtime reservation INSERT yetkisi (tüm stok azaltan yazıcılar kapasite protokolüne bağlanana kadar).
- Satış confirm endpoint'ine rezervasyon bağlanması, lot/seri, sevk tüketimi veya stok hareketi.
- API/web/Android.

## Milestone'lar

| No | Dikey dilim | Doğrulama | Durum |
|---:|---|---|---|
| 1 | Reservation lifecycle domain | Demand lineage, exact quantity, partial/full/release/expiry negatifleri | validating |
| 2 | Authorized warehouse-scoped candidate | Permission + authoritative warehouse evidence | validating |
| 3 | Sales → Inventory demand adapter | Published contract, exact scope/version ve kayıpsız line mapping | validating |
| 4 | Atomic available reservation persistence | PostgreSQL concurrency, forced RLS, idempotency, over-reservation block | in-progress |
| 4a | Internal writer + Sales/audit transaction composition | Kısmi/sıfır/replay, depolar arası talep sınırı, gerçek producer, audit ve caller rollback senaryoları yazıldı; runtime bekliyor | validating |
| 4b | Reservation/transfer ortak kapasite ve master koruması | İleri kesit minimumu, bloke/rezerv koruması, iki bacak rollback, master/ölçek ve writer contention senaryoları; runtime bekliyor | validating |
| 5 | Internal Sales confirm orchestration | Order version + tüm line snapshot + reservation + audit aynı transaction; runtime bekliyor | validating |
| 5a | Confirm bağlantısına hazırlık: tek-sipariş reservation batch | 1–500 satır, toplu sıralı kilitler, ikinci satır hatasında tüm paket rollback ve farklı sırada replay; runtime bekliyor | validating |
| 5b | Public confirm gateway/HTTP bağlantısı | Routing/DTO/hata ve unavailable DI eklendi; OpenAPI/SDK App Control engeli, SQL/runtime kapısı açık | in-progress |
| 5c | Sales-owned stock-order application gateway | Immutable line input, transaction-owned confirm/cancel, güvenli error mapping; unit/DB senaryoları hazır, runtime bekliyor | validating |
| 6 | Dispatch consumption ve iptal/release | Partial consume, source cancellation ve kalan release; otomatik expiry yok | in-progress |
| 6a | Yetkili manuel release ve atomik batch | Domain history rekonstrüksiyonu, consumed korunumu, replay/rollback/scope; runtime bekliyor | validating |
| 6b | Internal Sales cancellation + source discovery + release | Tüm kalanların atomik kapanması, replay/legacy/scope/audit senaryoları; runtime bekliyor | validating |
| 6c | Sıfır release iptalinde immutable tarih receipt'i | 0050 + aynı transaction receipt; tarih/legacy/rollback/RLS senaryoları derlendi, runtime bekliyor | validating |

## Açık politika sınırları

Güncel durum: DEC-MP01-025 aşağıdaki depo/expiry sorularını çözdü. Depo kullanıcı tarafından
seçilir, kısmi rezervasyon yapılır, otomatik expiry yoktur. Aşağıdaki sorular tarihsel bağlamdır;
miktar rezervasyonunda artık kullanıcı cevabı beklenmiyor. Değerleme ve backdate ayrı açık kalır.

- Depo rezervasyonda mı seçilecek, yoksa confirm anında şirket politikasıyla otomatik mi çözülecek?
- Expiry kullanılacaksa süre ve uzatma yetkisi nedir?
- Reservation ile eşzamanlı stok issue/transfer ortak position lock protokolüne bağlanmalıdır; bu bir teknik uygulama bağımlılığıdır, kullanıcı ürün kararı değildir.

Bu sorular ilk domain lifecycle'ı bloklamaz; authoritative persistence ve satış confirm orchestration başlamadan yanıtlanmalıdır.

## İlerleme günlüğü

- 10 Eylül 2026 son maliyet/commit isteği: Kullanıcı eksi stok çıkışında son bilinen maliyeti açıkça onayladı; karar kaydı, Inventory sözleşmesi ve kapanış değerlendirmesi güncellendi. Bu oturum yalnız politika kaydı ve birikmiş MP-04 çalışmasının commit/push teslimidir; maliyet writer'ının uygulandığı iddia edilmez. Hiç maliyet geçmişi olmayan ürün/sonraki uzlaştırma ayrıntısı ayrıca açık kalır. Önceki dar build sonuçları geçerlidir; runtime testler/migration uygulaması ve OpenAPI imza engeli çözülmüş sayılmaz. Kullanıcı bu teslim için commit/push yetkisi verdi; production deploy veya SQL grant açılışı yoktur.

- 10 Eylül 2026 kullanıcı stok kararı: hareketli ortalama onaylandı; eksi miktar izni DEC-025'in sevk yasağını supersede etti (rezervasyon kısmi/eldeki miktar sınırı korunur). Sayım farkı yetki verilen kullanıcı tarafından işlenebilir; otomatik ikinci onay varsayılmaz. Yönetici düzeltmesi original/audit korunarak karşı kayıt şeklinde yorumlandığı kullanıcıya bildirildi. Negatif stok geçici maliyeti ve backdate/reopen ayrıntısı onaylanmadığından DEC-011 bütün olarak evidence-received'dır; finansal posting bu ayrıntılarda varsayımla ilerlemez. Writer'lardaki eski koruma bu belge değişikliğiyle fiilen kaldırılmış sayılmaz.
  - Blokajsız hazırlık `INV-CNT-001`: `InventoryCountDifference` negatif book balance'ı kabul eden exact snapshot→physical count farkını hesaplar; fiziksel negatif sayım, geçersiz UTC cutoff ve numeric taşma reddedilir. Bu domain önizlemesi fiş/posting değildir; authoritative snapshot ve sonrasındaki hareket uzlaştırması persistence öncesinde gerekir. -5→0 (+5), -5→2 (+7), 10→8 (-2), eşit/sıfır ve 6 basamak senaryoları Unit harness'e eklendi.
  - İlk dar build'deki olmayan binary subtraction kullanımı mevcut addition/unary-negation value-object işlemleriyle düzeltildi. Unit Release `dotnet build tests/Unit/KaguERP.DomainUnitChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata (4,90 sn). Runtime testleri kullanıcı MP-sonu kadansı gereği çalıştırılmadı. Yeni SQL/API/yetki grant'i yok; MP-04 tamamlandı denmedi. OpenAPI imza engeli değişmedi; commit/push yok.

- 10 Eylül 2026 MP-04 bitirme isteği: [Faz kapanış değerlendirmesi](2026-09-10-mp04-closure-assessment.md) çıkarıldı. Rezervasyon alt diliminin ilerlemesi fazın bütünü değildir; kalıcı sevk/consume/değerleme, fatura/iade, web ve miktar/değer–GL golden kanıtı eksiktir. DEC-MP01-011 açık mali politika kararı kullanıcıya sunulur; varsayımla kapatılmaz. Windows imza politikası engeli sürer. Bu değerlendirmede yeni production davranışı/test/migration uygulaması yok; MP-04 completed yapılmadı.

- 10 Eylül 2026 tekrar güvenliği oturumu (`SALES-RES-007`, MP-04, R4): Kaynak incelemesi sıfır release iptalinde effectiveDate'in saklanmadığını doğruladı. Amaç/done when: rezervasyon olmayan iptal dahil aynı key/farklı tarih reddedilmeli; legacy geçmişe tarih uydurulmamalı; order/release/receipt/audit birlikte rollback olmalı. API/data architecture ve Sales sözleşmeleri üzerinden yeni ürün kararı gerektirmeyen idempotency düzeltmesi yapıldı.
  - `0050_sales_stock_cancellation_receipt` expand migration, Sales-owned writer ve cancellation composition bağlantısı eklendi. Scope/order/correlation FK'si, yalnız cancel event trigger'ı, UPDATE/DELETE yasağı, forced RLS ve SELECT-only runtime yetkisi var. Append-only kayıt yeni stok/GL olayı değildir. Eski kayıtlar backfill edilmez; receipt'siz replay `SALES_CANCEL_RECEIPT_CONFLICT` üretir ve gateway bunu 409'a eşler. Sipariş cancellation gerekçesi 500 sınırına (mevcut domain/DB) çekildi; manuel inventory release'in 1000 sınırı değiştirilmedi.
  - Test hazırlığı: boş iptal/date replay çatışması, receipt'siz legacy boş iptal, audit/release hatasında receipt rollback, owner UPDATE reddi, cancel dışı transition reddi, forced-RLS/SELECT-only catalog kontrolü; committed receipt için gerçek app hesabıyla doğru company görünür, farklı company/tenant görünmez senaryoları. Unit 500/501 gerekçe sınırı eklendi. Testler çalıştırılmadı; SQL/migration ve concurrency/runtime kanıtı MP-04 kapısında açık borçtur.
  - Doğrulama: Integration Release son build 0 uyarı/0 hata (6,71 sn), Bootstrap Release 0/0 (2,41 sn), Unit Release 0/0 (3,95 sn), komut biçimi `dotnet build <project> -c Release --no-restore -v:q`. Windows CodeIntegrity olay 3077 imza/politika engelini doğruladı; güvenlik ayarı değiştirilmedi, engelli OpenAPI yüklemesi tekrarlanmadı. API/Architecture/OpenAPI/SDK kapısı hâlâ açık, dar build'ler bunun yerine geçmez.
  - Migration henüz uygulanmadı; rollout expand-only, eski binary veri korunarak dönebilir fakat receipt'siz cancellation'lar yeni compound retry sayılmaz. Destructive down/grant/deploy/dependency/secret/commit/push yok. Sonraki adım güvenlik politikasına uygun OpenAPI/SDK doğrulaması; yazım kapısı tamamlanmadan açılmaz. Master faz kapısı ilerlemedi.

- 10 Eylül 2026 HTTP oturumu (`SALES-RES-006`, MP-04, R4): [HTTP sözleşme ve devreye alma notu](2026-09-10-sales-stock-http-contract.md) eklendi. Confirm/cancel mevcut action rotasında compound gateway'e yönlendirildi; diğer eylemler lifecycle gateway'de kaldı. Bootstrap yalnız unavailable stock gateway kaydeder, DB ayarı bunu açmaz; lifecycle-only fallback kaldırıldı. Yeni date/selection alanları, string decimal sonuçları, strict quantity/ETag parsing ve 403/404/409/412/422/503 eşlemesi eklendi. Eski confirm/cancel payload'ı ile uyumluluk iddiası yoktur; release/istemci uyumluluğu kapısı bekler.
  - Handler contract ve registration senaryoları yazıldı: yanlış decimal/ETag, duplicate satır, yanlış action alanları, inventory permission ve denied audit, immutable conflict/version, safe response ve fallback yokluğu. OpenAPI testleri yeni DTO'ları zorunlu kılar. Runtime çalıştırılmadı; yeni Architecture testlerinin son derlemesi henüz kanıtlanmadı.
  - Normal API Release build OpenAPI loader'da Windows Application Control `0x800711C7` nedeniyle 12 hata ile durdu; güvenlik ayarı/kapı değiştirilmedi. OpenAPI ve TS/Kotlin generated dosyaları güncel değildir; eski şemadan üretim yapılmadı. Bootstrap dar Release build 0 uyarı/0 hata (4,55 sn). MP-04 runtime/SQL/RLS/golden kapısı kullanıcı test kadansı nedeniyle çalıştırılmadı; auth ve stok doğruluğu kanıtlanmış sayılmadı. Migration/grant/dependency/secret/deploy/commit/push yok. Sonraki adımlar bağlı notta; master kapısı ilerlemedi.

- 10 Eylül 2026 servis dilimi doğrulaması: Bootstrap Release 0 uyarı/0 hata (7,88 sn); Integration Release son build 0 uyarı/0 hata (5,20 sn); Unit Release 0 uyarı/0 hata (3,09 sn). Komutlar `dotnet build <project> -c Release --no-restore -v:q`; ilk Unit build'deki CA1806 test constructor ifadeleri açık discard ile düzeltildi, analizör kapatılmadı. Runtime executable/DB/migration testleri kullanıcı MP-sonu kadansı gereği çalıştırılmadı; auth ve finansal miktar doğruluğu yalnız build ile kanıtlanmış sayılmadı. Önceki geniş OpenAPI/App Control engeli bu sonuçlarla çözülmüş değildir.

- 10 Eylül 2026 geniş servis oturumu (`SALES-RES-005`, MP-04, R4): `ISalesStockOrderGateway`, immutable bounded confirm/cancel komutları ve Sales-owned sonuç sözleşmeleri eklendi. Bootstrap implementation kendi ReadCommitted transaction'ında internal confirm/cancel composition'ını çağırır; server-derived request/source kimlikleri ve order-line ID üzerinden sonuç eşlemesi kullanır. Commit öncesi sonuç dönmez; SQL ayrıntısı, credentials veya serbest belge metni hataya taşınmaz. Belirsiz commit hatasında aynı key ile retry gereği tanımlandı; OperationCanceledException korunur.
  - Done when: input snapshot/limit/decimal/geçiş kontrolleri, permission/audit scope ön kontrolü, aynı-transaction persistence ve safe result/error mapping derlenir; test senaryoları ortak kapıya eklenir. Runtime kanıtı olmadan endpoint/grant/production tamamlandı denmez.
  - Unit senaryoları: liste mutasyonu, boş/duplicate/null satır, boş depo/line ID, 500/501 sınırı, quantity hassasiyeti, version overflow ve cancellation reason/transition. Gerçek DB senaryoları: gateway-owned commit'in bağımsız bağlantıda görünmesi, audit SQL hatasında order/result/creation/audit rollback, runtime app hesabına owner fallback yapılmaması, actor/permission retleri, immutable replay, değiştirilmiş miktar çatışması, cancel rollback/commit/replay ve SQL detayının maskelenmesi. Yeni fixture disposable integration DB'de committed seed kullanır; production verisi değildir. Senaryolar çalıştırılmadı.
  - Yeni endpoint, DI registration, OpenAPI değişikliği, migration, paket veya grant yok. Audit internal composition'da kalır; ekstra audit tekrarı eklenmedi. Stok/GL hareketi yaratılmaz. Önceki binary'ye dönüş veri koruyan rollback'tir. Mevcut kullanıcı değişiklikleri korundu; commit/push yapılmadı.
  - Sıradaki teknik işler: HTTP input/Problem Details yönlendirmesi ve generic confirm/cancel'ın compound yol ile tutarlılığı, MP-04 runtime SQL/RLS/concurrency kapısı ve buna bağlı sınırlı runtime grant; sevk consumption ayrıca açık. DEC-MP01-025 miktar işi için yeterli, yeni kullanıcı kararı istenmedi. DEC-MP01-011 maliyet/GL/backdate sınırı korunur. Faz kapısı değişmedi.

- 10 Eylül 2026 iptal oturumu son doğrulaması: `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata (8,18 sn); `dotnet build src/Erp.Bootstrap/KaguERP.Bootstrap.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata (2,59 sn); `git diff --check` temiz. Domain davranışı, SQL ve authorization/concurrency senaryoları derlendi ancak runtime olarak çalıştırılmadı; kullanıcı test kadansı gereği MP-04 birleşik kapısında doğrulanacak. Açık DB/performance kanıtı (500 limit dahil) nedeniyle completed/production hazır denmedi. Önceki geniş OpenAPI/App Control engeli bu dar kontrollerle çözülmüş sayılmadı.

- 10 Eylül 2026 geniş iptal oturumu (`SALES-RES-004`, `INV-RES-014`, MP-04, R4): Kullanıcıya MP-00/02 completed, MP-03 validating, MP-04 in-progress durumu aktarıldı. Genel yaklaşık %30 önceki mühendislik tahmini korundu; bu ölçülmüş kabul oranı değildir ve yeni iç kodla otomatik yükseltilmedi. Sipariş/rezervasyon miktar zincirinin sunucu kodu ilerlese de sevk/fatura/iade, production web/Android ve pilot hâlâ önündedir.
  - `PostgresInventorySourceReservationLoader` source type/id üzerinden tüm company-scoped reservation head'lerini Inventory sahipliğinde keşfeder. Depo kapsamı eksikse küme kısmen döndürülmez. 500 üstü geçmiş explicit typed limit ile kapalıdır; yüksek hacim desteği production öncesi teknik borçtur.
  - `PostgresSalesOrderCancellationOrchestrator`, Sales cancellation, kaynak keşfi, kalanlar için deterministik correlation'lı batch release ve audit'i aynı savepoint'e bağlar. Sales row yeni iptalde önce kilitlenir; yeni confirmed-demand okumasıyla reservation yarışı engellenir. Manuel release yarışı stale version üretirse bütün cancellation geri alınır. Kaynak stok/GL değiştirilmez. Legacy iptalde kalan aktif rezervasyon replay adıyla sonradan kapatılmaz. Mevcut cancel ve release permission'ları birlikte aranır; otomatik yükseltme/rol değişikliği yoktur.
  - Eklenen gerçek DB senaryoları: iki satırdaki 6 reserved miktarın iptalle kapanması; audit SQL hatasında order/transition/release/audit rollback; release yetkisi yokluğu; lifecycle'dan önce effective date; depo scope süresinin dolması; farklı şirket source UUID'si; legacy cancellation çatışması; önceden manuel bırakılmış satırın atlanması; immutable replay ve değişen effective-date ret; physical stock'un 6 kalması; rezervasyonsuz draft cancel/replay; deferred FK kapısı. Çalıştırılmadılar; MP-04 toplu runtime kapısı bekliyor.
  - Sınırlar: yeni migration/paket/secret/API/OpenAPI/role grant yoktur; mevcut HTTP cancel/confirm uçları değiştirilmedi. Tabloların runtime INSERT kapısı korunur. Dış entegrasyon/outbox olayı eklenmedi. Audit reason metnini çoğaltmaz. Kısmi sevk, maliyet, GL reversal veya kapalı dönem politikası seçilmedi. Eski binary'ye dönüşte tablo/geçmiş korunur; destructive down yok. Master faz kapısı ilerletilmedi; commit/push yapılmadı.
  - Sonraki dilim: public command sözleşmesi ve yetki/error mapping hazırlığı; miktar zinciri devreye alınmadan MP-04 runtime kanıtı ve source cancellation'ın mevcut gateway'e tutarlı yönlendirilmesi zorunludur. Dispatch consumption/valuation hâlâ ayrı tamamlanmamış kapsamdır.

- 10 Eylül 2026 sonraki geniş oturum (`INV-RES-012/013`, MP-04, R4): Kullanıcı karar kontrolünde miktar/manuel release için DEC-MP01-025'in yeterli olduğu doğrulandı; yeni ürün kararı istenmedi. Stok değerleme, negatif stok istisnası, backdate/repost ve sayım kararları DEC-MP01-011 altında açık tutuldu. `InventoryReservationReleaseRequest`, authoritative scope + demand/position kilitli `PostgresInventoryReservationReleaseWriter`, audit orchestration ve toplu release eklendi. Creation + version sıralı olaylar domain'den yeniden yürütülür; tüketim korunarak yalnız aktif kalan append-only release ile kapatılır. Tekrarda DB'deki ilk event/time/miktar döner. Batch bütün talepleri/pozisyonları önce kilitler ve ikinci satır/audit hatasında önceki release'leri de geri alır.
  - Test senaryoları: 10 reserved → 6 consumed → writer ile 4 released; stale version, blank reason, geçmiş lifecycle tarihi, yetkisiz kullanıcı, atanmamış depo, bulunmayan reservation ve changed-reason correlation retleri; audit SQL hatasında event rollback; terminal release reddi ve immutable replay; iki depodaki 5+5 reservation'ın toplu release'i, ikinci expected-version hatasında tüm event/audit rollback, ters sırada cevap/replay ve duplicate reservation reddi. Önceki lifecycle/capacity senaryosu artık sentetik release INSERT yerine gerçek release orchestration'ını çağırır.
  - Kapsam/sınır: manuel release permission'ı açık sözleşmedir; kullanıcı/rol grant'i yapılmadı. Yeni DB şeması/paket/API/OpenAPI/GL/stok hareketi yok; lifecycle INSERT SELECT-only sınırı devam eder. Gerekçe yalnız scope'lu lifecycle tablosunda saklanır, audit'e taşınmaz. Caller commit/rollback sahibidir. Önceki binary'ye dönüşte veri korunur; destructive rollback yok. Domain geçmiş kontrolü ve DB constraint kapıları zayıflatılmadı.
  - Sonraki kesin adım: Inventory-owned kaynak sipariş rezervasyon keşfi ve Sales cancellation'ı kalan release'lerle atomik bağlamak; manuel batch tek başına sipariş iptali değildir. Public confirm/cancel yolları bu bağlantı ve runtime grant/test kapısı tamamlanmadan açılmaz. Master kapısı ilerletilmedi; commit/push yapılmadı.
  - Doğrulama: Integration Release `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata (8,51 sn); son production kodu Bootstrap Release `dotnet build src/Erp.Bootstrap/KaguERP.Bootstrap.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata (3,08 sn). `git diff --check` temiz. Runtime SQL, concurrency, RLS/authorization, migration ve golden testler kullanıcı kadansı gereği MP-04 sonunda çalıştırılacak; bu derlemeler runtime kanıtı değildir. Önceki OpenAPI/App Control engeli giderilmiş sayılmadı.

- 10 Eylül 2026 doğrulama: Son kod `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` ile 0 uyarı/0 hata (8,10 sn); gerçek composition projesi `dotnet build src/Erp.Bootstrap/KaguERP.Bootstrap.csproj -c Release --no-restore -v:q` ile 0 uyarı/0 hata (4,11 sn). `git diff --check` temiz. Test executable'ları, PostgreSQL senaryoları ve migration çalıştırılmadı; financial/authorization/concurrency runtime doğrulaması kullanıcı MP-sonu kadansı gereği bekler. Önceki OpenAPI/Windows Application Control engeli bu dar derlemelerle çözülmüş sayılmadı; güvenlik ayarı veya kapı değiştirilmedi.

- 10 Eylül 2026 geniş oturum (`SALES-RES-003`, MP-04, R4): Önceki teslimlerdeki tarih/değerleme kapısı ifadesi daraltıldı. DEC-MP01-025/proposal miktar persistence'ına açıkça izin verir; DEC-MP01-011 maliyet/GL/backdate posting kararlarını bloklar, internal miktar confirm composition'ını değil. `PostgresSalesOrderConfirmationOrchestrator` approved→confirmed geçişini, siparişin tüm satırları için batch reservation/result ve audit ile aynı savepoint'e bağlar. Eksik/tekrarlı satır, scope/version uyumsuzluğu, stok master hatası veya audit hatası header/transition dahil geri alınır. Kapasite yokluğu hata değil immutable kısmi/sıfır sonuçtur.
  - Request gate'leri Sales row update'inden önce alınarak mevcut batch ile kilit sırası korunur. Confirmation correlation + line ID için sabit SHA-256/UUIDv8 request ID, aynı onayı yeni key ile yeniden rezervasyon üretmekten korur. Değişen quantity/warehouse/date canonical fingerprint conflict verir. Önceden reservation olmadan confirm edilen legacy çağrı bu orchestration'ın replay'i diye sonradan tamamlanmaz; generic conflict üretir.
  - Gerçek DB senaryoları hazırlandı: 6 talebe 3 stokla atomik partial/zero confirm; eksik satır ve ikinci satırda kesirli adet reddinde approved header/version ve audit/result/event rollback; audit DB hatasında tüm rollback; ters sırada immutable replay; aynı onayda miktar değişikliği ve rastgele request ID reddi; reservation sonucu olmayan legacy confirmation ret; deferred FK kapısı. SQL testleri bu oturum çalıştırılmadı; MP-04 kapanışına kayıtlı borçtur.
  - Sınır: mevcut HTTP confirm yolu değiştirilmedi, INSERT grant açılmadı, migration/paket/secret/PII değişikliği yok. Stock-only yol servis/lot/seri siparişleri için hazır sayılmaz. Audit teknik kimlik taşır. Commit/rollback caller'a aittir; eski binary'ye dönüş veri silmez. Sonraki blokajsız iş DEC-MP01-025 kapsamında iptal/manuel release lifecycle bağlantısıdır; public confirm iptal/release ve runtime yetki/test kapısı tamamlanmadan açılmaz. Master faz kapısı değişmedi; commit/push yok.

- 9 Eylül 2026 ikinci geniş oturum doğrulama/teslim: kapasite/master dilimi sonrası Architecture Release derlemesi 0 uyarı/0 hata (22,72 sn). Batch dahil son geniş Architecture denemesi OpenAPI üreticisinin API DLL'ini yüklemesinde Windows Uygulama Denetimi `0x800711C7` ile başarısız oldu (7 hata); güvenlik politikası değiştirilmedi veya kapı devre dışı bırakılmadı. Son kod ve tüm yeni fixture'lar `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` ile 0 uyarı/0 hata (14,98 sn) derlendi. Bu proje API/OpenAPI üreticisini çalıştırmaz; geniş kapının yerine geçtiği iddia edilmez. `git diff --check` temiz. SQL/migration, DB concurrency, authorization ve golden runtime senaryoları kullanıcı kararıyla MP-04 sonuna ertelendi; sonuçları henüz kanıtlanmadı. Sonraki kapı: çok-satırlı paket hazır; confirm endpoint'ine geçmeden DEC-MP01-011/tarih politikası ve runtime yayın sınırı çözülmeli. Commit/push yapılmadı.

- 9 Eylül 2026 ikinci geniş oturum devamı (`SALES-RES-002`): `PostgresSalesOrderReservationBatch` eklendi. Tek sipariş/sürüm/tarih/actor ve 1–500 tekil satır doğrulamasından sonra bütün request gate'leri, ortaklaştırılan `PostgresInventoryDemandLock` ile bütün talep kilitlerini ve bütün pozisyon kilitlerini yazmadan önce alır. Demand hash anahtarı önceki tek-satır uygulamayla birebir korunur; version/depo kilidi bölmez. Sonuçlar girdi sırasına geri yerleştirilir; her satır/audit aynı outer savepoint içindedir. Gerçek iki-satırlı Sales draft→submit→approve→confirm fixture'ı, 6 stoktan 3+3 ayırma, ikinci satırda geçersiz kesir nedeniyle ilk reservation/result/audit'in geri alınması, paketin ters sırada replay'i, mükerrer satır reddi ve deferred FK doğrulaması için senaryolar eklendi. Bu confirmed talebi tüketen transaction katılımcısıdır; confirm endpoint'i hâlâ değiştirilmedi. Batch için ayrıca kullanıcı kararı, yeni izin, paket, migration veya API açılışı yoktur.

- 9 Eylül 2026 ikinci geniş oturum (`INV-CAP-001/002`, MP-04, R4): `PostgresInventoryPositionCapacityLoader` ile stok/lifecycle/bloke değişim tarihleri boyunca minimum kapasite ve protected deficit projeksiyonu eklendi. Reservation create bu minimumla sınırlandı. Immediate transfer yeni çift yazdıktan sonra aynı position kilitleri altında rezervasyon/bloke stok karşılığını doğrular; kapasite veya ikinci INSERT hatasında iki bacak da savepoint'ten geri alınır. Bekleme sonrası depo yetkisi yeniden okunur. `PostgresInventoryStockMasterLoader` iki yolda aktif item/company/warehouse, stock/untracked ve miktar ölçeği için ortak FOR SHARE denetimidir; replay güncel yetkiyle orijinal sonucu korur.
  - Yeni DB senaryoları: ileri issue −4 ve daha sonraki receipt +10 arasında rezervasyonun 1 ile sınırlanması; ileri recorded timestamp'in güvenlik kontrolünde gizlenmemesi; gelecekteki bloke nedeniyle transfer reddi; 5 rezerv + 2 bloke bulunan 7 stoktan çıkış reddi; diğer depodaki serbest 2'nin transferi ve sıfır serbest stokta replay; fazla çıkış, kesirli adet ve pasif hedef depo reddi; ikinci bacak unique hatasında ilk bacağın rollback'i; farklı talep rezervasyonu ve transfer yazıcısının ortak position kilidinde beklemesi; effective bloke release sonrası kapasitenin açılması.
  - Sınır: INV-CAP iç güvenlik projeksiyonudur, tarihli rapor değildir; mevcut gelecekteki taahhütleri rapor cutoff'uyla gizlemez. Negatif stok istisnası, geçmiş tarihli/kapalı dönem posting izni veya maliyet kuralı seçilmedi. Runtime reservation INSERT/HTTP/DI kapısı açılmadı. Yeni migration/paket/izin/kişisel veri veya OpenAPI değişikliği yoktur. Ortak master ve kapasite hataları hassas stok miktarı içermez; transfer audit'i ileride public posting orchestration transaction'ında bağlanmalıdır.
  - Done when: yeni kod ve DB senaryoları derlenmiş, davranış sözleşmesi ve plan güncel; gerçek concurrency/RLS/SQL sonuçları MP-04 toplu kapıda kanıtlanmadan milestone completed sayılmaz. Eski binary'ye dönüş şemayı/veriyi korur. Bir sonraki uygulama bağımlılığı confirm komutunun çok satırlı kilit sırası ve tarih/politika giriş kapısıdır; DEC-MP01-011 kararı sessizce kapatılmaz. Commit/push yok; master faz kapısı değiştirilmedi.

- 9 Eylül 2026 geniş dilim son doğrulaması: `dotnet build tests/Architecture/KaguERP.ArchitectureChecks.csproj -c Release --no-restore -v:q` son kod durumunda 0 uyarı/0 hata (18,89 sn); `git diff --check` temiz. İlk derleme denemelerinde görülen Bootstrap doğrudan proje referansları ve architecture harness yeni fixture Compile-link eksikleri giderildi. Yeni PostgreSQL senaryoları derlendi ancak çalıştırılmadı; migration, concurrency ve güvenlik runtime kanıtı MP-04 toplu test kapısında açık kalır.

- 9 Eylül 2026 geniş uygulama dilimi (`INV-RES-011`, R4): Request gate → aynı-transaction Sales published demand → demand/position lock → active item/company/warehouse + miktar ölçeği → on-hand/active/blocked ve kalan talep → atomik creation/result akışı `PostgresInventoryReservationWriter` ile birleştirildi. Pozitif ve sıfır sonuç aynı immutable replay yolundadır; iki depoya bölünen rezervasyon toplamı kalan talebi aşamaz. Inventory Sales tablolarına erişmez. `PostgresSalesOrderReservationOrchestrator` üreticiyi aynı connection/transaction ile kurar ve audit'i aynı transaction'a ekler; audit hatası savepoint üzerinden rezervasyon sonucunu da geri alır. Master kartlar FOR SHARE kilitlidir; lot/seri ve servis ürünleri bu ilk yolda kapalıdır. Otomatik expiry/GL/stok hareketi üretilmez.
  - Yazılan PostgreSQL senaryoları: 7 fiziksel − 2 bloke = 5 rezervasyon, replay/conflict, ikinci istekte sıfır, diğer depoda kalan talep 5, talep dolduktan sonra sıfır, kesirli adet reddi, deferred creation/result FK doğrulaması, gerçek confirmed Sales kaynağıyla composition, audit INSERT hatasında rollback ve caller rollback.
  - Güvenlik sınırı: şema runtime SELECT-only kalır; yeni API/DI kaydı/INSERT grant açılmadı. Bu dilim test edilebilir transaction katılımcısıdır, kullanıcıya açık tamamlanmış rezervasyon modülü değildir. Mevcut generic transfer yazıcısının negatif stok/rezervasyon koruma politikası ve backdate kapısı tamamlanmadan runtime açılmaz. Owner bağlantısı yalnız test fixture'ında kullanılır, uygulama bağlantısına yükseltilmez.
  - Deployment/geri dönüş: yeni bağımlılık paketi yok; Bootstrap'a mevcut Inventory/Sales contract proje referansları eklendi. Önceki binary'ye dönüşte ek tablolar korunur; veri silme/down migration yok. API değişmediğinden OpenAPI ve istemci üretimi gerekmez. Audit yalnız teknik request kimliği taşır; miktar/belge içeriği loglanmaz.
  - Sıradaki kesin adım: stok azaltan yollarda aynı position-lock altında rezervasyon/bloke kapasitesini korumak, tarih kapısını uygulamak ve ardından confirm komutunun depo seçimiyle runtime gateway/endpoint bağlantısını açmak. MP-04 çıkış kapısı ilerletilmedi; commit/push yapılmadı. Runtime DB/migration/finansal-güvenlik kanıtları kullanıcının MP-sonu test kararı gereği hâlâ bekliyor; derleme bunların yerine geçmez.

- 9 Eylül 2026 doğrulama: `dotnet build tests/Architecture/KaguERP.ArchitectureChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata; `git diff --check` temiz. SQL ve DB testleri henüz çalıştırılmadı; migration resource/C# derlemesi runtime kanıtı değildir.

- 9 Eylül 2026: `INV-BLK-001` altında `0049_inventory_stock_block` expand migration eklendi. Company/item/UOM/warehouse/actor FK'leri, forced RLS, immutable sürüm zinciri, correlation unique ve azalan remaining/terminal guard'larıyla bloke miktar kaydı kuruldu. Runtime SELECT-only; yeni yazma yetkisi veya API açılmadı. Balance loader aynı snapshot'ta last-effective block toplamını döndürür. DB senaryosu 3 bloke → 2 kalan → gelecek tarihli 0 geçişinde bugünkü blocked=2, miktar artırma ret davranışını kapsar. Runtime/migration uygulaması MP-04 sonuna ertelendi. Rollback eski binary'ye dönüp ek tabloyu korumadır; destructive down yok. Bloke kayıtları stok hareketi/GL değildir. Sıradaki adım aktif master/quantity-scale ve capacity kontrollerini atomik reservation create writer'a bağlamak; bütün writer'ların position lock protokolüne katılması zorunludur. Commit/push yapılmadı; master faz kapısı değişmedi.

- 8 Eylül 2026 doğrulama/teslim: Architecture ve Integration `dotnet build ... -c Release --no-restore -v:q` derlemeleri 0 uyarı/0 hata; `git diff --check` temiz. SQL ve DB senaryoları çalıştırılmadı. Kullanıcıya genel kapsam için yaklaşık %30, MP-04 için yaklaşık ilk üçte bir şeklinde mühendislik tahmini verildi; bunlar ölçülmüş kabul/test oranları değildir. MP-00/02 completed, MP-03 validating, MP-04 in-progress durumu esas alınır.

- 8 Eylül 2026 lifecycle persistence: `0048` immutable consume/release event tablosu eklendi; exact ardışık version, terminal-state reddi, pozitif consume/zero release, cumulative consumed ve remaining korunumu DB trigger'ıyla tanımlandı. Company/actor/creation FK, forced RLS, correlation uniqueness ve runtime SELECT-only sınırı korunur. Balance loader effective/recorded kesitinde son olaydan aktif rezervasyonu, demand için consumed+remaining toplamını döndürür. Sentetik owner transaction senaryosu 10 rezervasyon → 6 consume → 4 release; aktif 0, demand taahhüdü 6 ve fazla/terminal tüketim retlerini kapsar. Migration expand-only; önceki binary'ye dönüşte tablo/veri korunur, down/delete yok. Runtime/migration uygulama MP-04 toplu kapısında. Bloke stok kanıtı ve atomik yazıcı bağlantısı hâlâ açık; bu tablolar gerçek sevk posting'i açmaz. Master kapısı değişmedi; commit/push yapılmadı.

- 8 Eylül 2026 balance doğrulaması: `dotnet build tests/Integration/KaguERP.DatabaseIntegrationChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata; `git diff --check` temiz. PostgreSQL sorgusunun çalışma zamanı kanıtı toplu kapıda bekliyor.

- 8 Eylül 2026 balance dilimi: `PostgresInventoryReservationBalanceLoader` eklendi; exact scope/source/version ve azami demand miktarı doğrulanır. Request gate sonrasında version/depo bağımsız source-line lock ve position lock alınır; beklemeden sonra warehouse yetkisi yeniden doğrulanır. DB cutoff ile on-hand, position ve demand bazında brüt creation toplamları tek sorguda okunur. Zero-result request'in stok ayırmadığı ve boş toplamlar app-rolü DB senaryosuna eklendi. Runtime MP-04 sonuna ertelendi. Brüt creation toplamı active reserved değildir; tüketim/release ve blocked kaynağı olmadan bu snapshot'tan production available üretilmez. Sıradaki zorunlu adım reservation lifecycle persistence ve blocked quantity evidence; ardından atomik create writer açılabilir. INSERT yetkileri kapalı, yeni API/migration yok; master kapısı değişmedi. Commit/push yapılmadı.

- 8 Eylül 2026 request gate: `PostgresInventoryReservationRequestGate` tenant/company/request kimliğiyle transaction lock alır; fingerprint/depo değişimi farklı lock üretmez. ReadCommitted ve aynı connection/transaction zorunlu, authoritative warehouse yetkisi kilitten önce ve sonra denetlenir. Replay fresh statement snapshot ile okunur. İki bağlantılı gerçek DB senaryosu farklı miktarla lock timeout, owner commit sonrası app rolüyle zero replay ve conflict davranışını kapsar. Fixture owner yazımı yalnız test içindir; runtime INSERT hâlâ kapalıdır. Integration Release build 0 uyarı/0 hata; runtime MP-04 sonuna ertelendi. Sıradaki adım request kilidi sonrasında demand kilidi ve authoritative kapasite/yazma; null replay kapasite veya yazma izni değildir. Migration/API değişmedi; commit/push yapılmadı.

- 8 Eylül 2026 replay dilimi: `InventoryReservationRequest` versioned canonical JSON/SHA-256 fingerprint ve permission/company sınırıyla eklendi. `PostgresInventoryReservationReplayLoader` caller transaction'ında authoritative warehouse scope'u yenileyip immutable istek sonucunu okur; sıfır sonuç değişmeden döner, farklı içerik typed conflict'tir. Unit decimal normalization/effective-date farkı ve DB harness sıfır replay/değişmiş miktar conflict senaryoları eklendi. Bu DB fixture migrator transaction'ında çalışır; runtime rolü RLS izolasyon kanıtının yerine geçmez. Unit ve Integration Release derlemeleri 0 uyarı/0 hata; runtime MP-04 sonunda. Yeni migration yok. Writer ve API açılmadı; request kilidi altında ikinci kontrol ve kapasite hesap/yazımı sıradaki adımdır. Master kapısı değişmedi; commit/push yapılmadı.

- 8 Eylül 2026 sonuç: Architecture Release build (`dotnet build tests/Architecture/KaguERP.ArchitectureChecks.csproj -c Release --no-restore -v:q`) 0 uyarı/0 hata; `git diff --check` temiz. Yeni SQL PostgreSQL üzerinde henüz uygulanmadı; derleme runtime constraint/transaction doğrulaması değildir.

- 8 Eylül 2026 devam: `0047_inventory_reservation_request_result` migration'ı sıfır/kısmi sonuç ve fingerprint kaydını ekledi. Company/request PK, pozitif sonuç için exact creation bağlantısı, reciprocal deferred FK ve sıfır sonuç/pozitif creation çelişkisini reddeden çift trigger ile commit atomikliği tasarlandı. Her iki tablo runtime SELECT-only kalır. Sentetik DB senaryosu sıfır sonuç, duplicate key ve owner seviyesinde immutable UPDATE reddini kapsar; runtime MP-04 toplu kapısına bırakıldı. Expand migration eski binary ile uyumlu; geri dönüş veriyi koruyarak önceki binary'dir, down/delete yok. API veya stok yazımı açılmadı. Sıradaki adım canonical fingerprint/replay ve kilit altında capacity writer; şema varlığı bunların tamamlandığı anlamına gelmez. Master kapısı değişmedi; commit/push yapılmadı.

- 8 Eylül 2026 dar doğrulama: `dotnet build tests/Architecture/KaguERP.ArchitectureChecks.csproj -c Release --no-restore -v:q` 0 uyarı/0 hata; `git diff --check` temiz. SQL henüz PostgreSQL üzerinde uygulanıp çalıştırılmadı; build yalnız C# ve migration resource derlemesini kanıtlar.

- 8 Eylül 2026: `0046_inventory_reservation_creation` expand migration eklendi. Inventory-owned immutable create snapshot; tenant/company, request uniqueness, item-company/UOM/depo/actor FK'leri, pozitif ve talebi aşmayan numeric miktar, effective date ve recorded timestamp, correlation ve policy sürümü taşır. Force RLS uygulanır; runtime SELECT-only olduğundan capacity writer hazır olmadan gerçek reservation yazılamaz. UPDATE/DELETE trigger'ı owner seviyesinde de hatalı yerinde düzeltmeyi reddeder. Catalog/RLS/privilege/constraint kontrolü gerçek DB harness'ine eklendi; runtime ve migration uygulaması MP-04 toplu kapısına bırakıldı. Yeni tablo önceki migration'ları değiştirmez; eski binary tabloyu kullanmaz, geri dönüş eski binary + tabloyu koruma şeklindedir. Veri silen down migration yoktur. Şema tek başına available veya idempotent replay garantisi değildir; zero-result request kaydı, lifecycle ve atomik writer sonraki dilimde tamamlanacaktır. API/audit/GL akışı açılmadı. Master kapısı değişmedi; commit/push yapılmadı.

- 7 Eylül 2026 uygulama: Kullanıcının Devam yanıtı DEC-MP01-025 olarak kaydedildi. `INV-RES-004` kısmi kapasite hesabı ve `INV-LOCK-001` transaction advisory lock eklendi; immediate-transfer writer iki pozisyonu birlikte kilitler. ReadCommitted dışında çalışma reddedilir; lock bütün transaction boyunca tutulur. Unit miktar korunumu/kesirli değer ve iki PostgreSQL bağlantısında lock timeout + rollback sonrası tekrar edinme senaryosu yazıldı. Unit ve Integration Release build 0 uyarı/0 hata; runtime kullanıcı kararıyla MP-04 sonuna ertelendi. Yeni migration yok; kalıcı reservation writer henüz eklenmedi. Sonraki adım kilit altındaki güncel stok/rezervasyon okumasıyla atomik create ve idempotency persistence. Kilit tek başına negatif stok engeli değildir. Commit/push yapılmadı.

- 7 Eylül 2026: Persistence öncesi açık ürün kararları [rezervasyon önerisinde](2026-09-07-reservation-policy-proposal.md) somutlaştırıldı: depo seçimi, yetersiz stokta kısmi rezervasyon ve otomatik süre sonu. Öneriler henüz kabul edilmedi. Teknik kilit protokolü ürün kararı listesinden ayrıldı. Davranış değişmedi; test/derleme çalıştırılmadı. Sıradaki bağımlı adım kullanıcı cevabıyla karar kaydını güncelleyip persistence kapsamını açmaktır.

- 7 Eylül 2026 doğrulama: `dotnet build tests/Architecture/KaguERP.ArchitectureChecks.csproj -c Release --no-restore -v:q` başarılı, 0 uyarı/0 hata; `git diff --check` temiz. Runtime testleri çalıştırılmadı. Faz kapısı değişmedi; sıradaki adım sevk hazırlama sözleşmesinin mevcut sipariş/rezervasyon bağımlılıklarıyla modellenmesidir. Commit/push yapılmadı.

- 7 Eylül 2026: `INV-RES-002/003` R4 yetki sırası düzeltmesi seçildi. Candidate builder Sales demand kaynağını çağırmadan permission, tenant/company ve actor-bound depo kanıtını ortak `EnsureAccess` kontrolünden geçirir; nihai candidate aynı kontrolü korur. Beş negatif senaryo (permission, tenant, company, actor ve atanmamış depo) için producer çağrı sayısının sıfır kalmasını denetleyen kontroller eklendi. Kabul ölçütü yetkisiz istekten producer okuması çıkmamasıdır. Yeni endpoint, migration, kayıt, audit olayı veya ekonomik sonuç yoktur; mevcut çağıran transaction/audit sorumluluğu korunur. Çalışma zamanı güvenlik kontrolleri kullanıcı kararıyla MP-04 toplu kapısına ertelenmiştir; derleme çalışma zamanı kanıtı sayılmaz.

- 5 Eylül 2026: Inventory-owned reservation state, versioned demand lineage, warehouse/base-UOM hedefi ve exact decimal consume/release/expiry geçişleri eklendi. Persistence ve available sonucu üretilmedi; bu nedenle eşzamanlılık veya over-reservation kanıtı henüz ileri sürülmüyor. Domain ve Unit Release derlemesi dar kapıdır; runtime MP-04 sonunda çalıştırılacaktır.
- 5 Eylül 2026: `INV-RES-002` authorization candidate eklendi. `inventory.reservation.create`, exact company scope ve transaction içinde authoritative olarak yüklenecek actor-bound warehouse evidence olmadan persistence adayı üretilemez. Candidate demand source'un gerçekten var olduğunu iddia etmez; bu doğrulama ileride Inventory orchestration'ın çağıracağı published module contract üzerinden yapılacaktır. Inventory Application ve Unit Release derlemeleri dar kapıdır; runtime MP-04 sonunda çalıştırılacaktır.
- 5 Eylül 2026: Candidate exact demand evidence ile sıkılaştırıldı. Evidence source type/id/line/version, item, base-UOM ve azami ayrılabilir miktarı taşır; stale source version, farklı ürün/birim veya talebi aşan reservation reddedilir. Evidence tipinin varlığı tek başına otorite değildir; persistence öncesi producer module published contract adaptöründen aynı transaction bağlamında yüklenmesi zorunlu kalır. Inventory Application ve Unit Release derlemeleri `0 warning/error`.
- 5 Eylül 2026: İlk producer contract olarak `Sales.Contracts` reservation-demand snapshot'ı ve Sales-owned PostgreSQL source eklendi. Yalnız confirmed exact order version ve `sales.order.confirm` permission/company scope altında satır yayımlar; Inventory Sales tablosu okumaz.
- 5 Eylül 2026: `INV-RES-003` Sales → Inventory demand adaptörü eklendi. Adaptör producer'ın döndürdüğü tenant/company/order/version bağlamını request ile birebir doğrulayıp her satırı Inventory quantity/UOM değer nesneleri üzerinden reservation evidence'a çevirir; null veya scope/version sapması fail-closed kalır. Exact order-line bulunmadan candidate kurulamaz; bulunan satırdan üretilen state ayrıca permission, company, actor-bound warehouse ve demand-capacity invariant'larından geçer. Inventory Infrastructure, Integration ve Architecture Release derlemeleri `0 warning/error`; runtime MP-04 toplu kapısına bırakıldı. Candidate builder persistence/available sonucu üretmez ve confirm orchestration hâlâ kapsam dışıdır.
